services:
  finance-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "0.0.0.0:80:80"
      - "0.0.0.0:443:443"
    volumes:
      - /etc/ssl/finance:/etc/nginx/ssl:ro
    restart: unless-stopped
    container_name: finance-frontend
